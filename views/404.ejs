<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 Not Found</title>
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            background: #2c3e50;  
            color: #ecf0f1;  
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
        }

        .container {
            text-align: center;
            animation: fadeIn 1.5s ease-in-out;
        }

        h1 {
            font-size: 5em;
            margin-bottom: 10px;
            color: #e74c3c; /* Red color */
            animation: bounceIn 1s ease-in-out;
        }

        p {
            font-size: 1.5em;
            margin-bottom: 30px;
            animation: fadeInUp 1.5s ease-in-out;
        }

        @keyframes bounceIn {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }

            40% {
                transform: translateY(-30px);
            }

            60% {
                transform: translateY(-15px);
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .geometric-pattern {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
            opacity: 0.5;
        }

        .geometric-pattern img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div class="geometric-pattern">
        <img src="https://source.unsplash.com/1920x1080/?geometric" alt="Geometric Pattern">
    </div>

    <div class="container">
        <h1>404 Not Found</h1>
        <p>Oops! The page you are looking for seems to have vanished.</p>
        <p>Take a detour to <a href="/">Home</a>.</p>
    </div>
</body>
</html>
